module.exports = {
  // build config
  // appPath: appPath,
  // distPath: distPath,
  // app: pathCreator(appPath),
  // dist: pathCreator(distPath),
  // debug: true,
  // isDevelopment: true,
  // isProduction: false,
  // sourcemaps: true,
  // rev: false,
  // uglify: false,
  // minifycss: true,
  // htmlmin: true,
  // autoreload: true,

  hosts: {
    api: 'development.com'
  }
};
